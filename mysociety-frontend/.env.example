# API Gateway base URL. The browser never connects to service databases.
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Set true only for local/demo development. Do not enable in production.
VITE_USE_MOCK_API=true
VITE_MOCK_LATENCY_MS=150
